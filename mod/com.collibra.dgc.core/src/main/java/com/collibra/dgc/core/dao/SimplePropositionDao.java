package com.collibra.dgc.core.dao;

import com.collibra.dgc.core.model.meaning.SimpleProposition;

public interface SimplePropositionDao extends AbstractDao<SimpleProposition> {
	// /**
	// * Check if the simple proposition is persisted or not.
	// * @param simpleProposition The {@link SimpleProposition}
	// * @return {@code true} if it is persisted, {@code false} otherwise
	// */
	// boolean isPersisted(final SimpleProposition simpleProposition);
}
