<script type="text/javascript">
var page = {
  resourceid: "$!{id}" // The current Resouce ID.
}

// load sea.js asynchronously:
;(function(m, o, d, u, l, a, r) {
  if(m[d]) return;
  function f(n, t) { return function() { r.push(n, arguments); return t; } }
  m[d] = a = { args: (r = []), config: f(0, a), use: f(1, a) };
  m.define = f(2);
  u = o.createElement('script');
  u.id = d + 'node';
  u.src = '$!{contextPath}resources/library/core/loader.js';
  l = o.getElementsByTagName('head')[0];
  l.insertBefore(u, l.firstChild);
})(window, document, 'seajs');

seajs.config({ 
  base: location.protocol + '//' + location.host + '$!{contextPath}/resources',
  alias: {
    "core": "$!{contextPath}resources/library/core/core.js"
  }
});

var core = {
      dom: {
        queue: [],
        ready: function(callback) {
          domqueue.push(callback);
        }
      },
      module: {
        use: function(mods, callback) {
          seajs.use(mods, callback);
        }
      }
    },
    domqueue = [];
    
</script>

##<!--[if lte IE 8]>
## <script src="$!{contextPath}resources/library/js/ie/IE9.js" type="text/javascript"></script>
##<![endif]-->