JSDOC.PluginManager.registerPlugin(
    "JSDOC.group",
    {
        onSymbol: function(symbol) {
            print(symbol);
        },
        onDocCommentSrc : function(symbol) {
            print(symbol);
        }
    }
);