package com.collibra.dgc.core.service;


/**
 * Specifies the capabilities of a scheduling in glossary.
 * @author amarnath
 * 
 */
public interface SchedulerService {
	// /**
	// * Scheduler for email notifications for specified user.
	// * @param user The user.
	// * @param cronExpression The cron string to trigger notifications.
	// */
	// void scheduleUserEmailNotification(String user, String cronExpression);
	//
	// /**
	// * Schedule email notification job.
	// * @param name The name of the job.
	// * @param group The group name.
	// * @param cronExpression Cron expression.
	// * @param Class jobClass The job class.
	// */
	// void scheduleEmailNotification(String name, String group, String cronExpression, Class<? extends Job> jobClass);
	//
	// /**
	// * @return The {@link Scheduler}.
	// */
	// Scheduler getScheduler();
	//
	// /**
	// * Unschedule from email notifications for specified user.
	// * @param user The user name.
	// */
	// boolean unscheduleFromUserEmailNotification(String user);
	//
	// /**
	// * Unschedule email notifications.
	// * @param name The job name.
	// * @param group The group name.
	// * @return True of successful otherwise false.
	// */
	// boolean unscheduleFromEmailNotification(String name, String group);
}
